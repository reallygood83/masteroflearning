# AI 교육 뉴스 플랫폼 - 완전한 시스템 사양서 (SPEC) v2.0

> **프로젝트명**: AI Edu News - Feynman Style
> **버전**: 2.0.0 (Neo-Brutalism Edition)
> **업데이트 날짜**: 2025-11-23
> **디자인 컨셉**: Neo-Brutalism (네오 부루탈리즘)
> **대상 사용자**: 교사, AI 유튜버, 교육 콘텐츠 크리에이터
> **목적**: 파인만 기법으로 AI/교육 뉴스를 자동 재작성하고 원클릭 발행

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [디자인 철학 - Neo-Brutalism](#2-디자인-철학---neo-brutalism)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [인증 및 권한 시스템](#4-인증-및-권한-시스템)
5. [데이터베이스 설계](#5-데이터베이스-설계)
6. [기능 명세](#6-기능-명세)
7. [단계별 개발 계획 (6 Phases)](#7-단계별-개발-계획-6-phases)
8. [API 명세](#8-api-명세)
9. [UI/UX 설계 (Neo-Brutalism)](#9-uiux-설계-neo-brutalism)
10. [보안 및 암호화](#10-보안-및-암호화)
11. [배포 및 운영](#11-배포-및-운영)
12. [비용 예측](#12-비용-예측)

---

## 1. 프로젝트 개요

### 1.1 비전 (Updated)

**"교사와 AI 유튜버가 복잡한 AI/교육 뉴스를 5분 안에 이해하고, 대담하고 현대적인 디자인으로 콘텐츠를 생산할 수 있게 한다"**

### 1.2 핵심 가치 제안 (v2.0)

- ✅ **소프트 랜딩**: 단계적 개발로 안정적 MVP 출시
- 🎨 **대담한 디자인**: Neo-Brutalism 스타일로 강렬한 시각적 임팩트
- 🔐 **보안 우선**: API 키 암호화 저장, 구글 로그인 기반 인증
- 🤖 **유연한 AI**: 관리자가 직접 xAI Grok API 설정 가능
- 👥 **계층적 권한**: 관리자/일반 사용자 명확한 분리
- 📧 **간편한 구독**: 이메일 입력만으로 뉴스레터 구독

### 1.3 타겟 사용자 페르소나 (Updated)

#### Persona 1: 김교사 (초등학교 교사, 35세)
- **니즈**: AI 교육 도구 활용법을 쉽게 이해하고 싶음
- **행동**: 구글 계정으로 간편 가입 → 뉴스 읽기 → 이메일로 뉴스레터 구독
- **기대**: 깔끔하고 현대적인 UI에서 복잡한 뉴스를 쉽게 이해

#### Persona 2: 박유튜버 (AI 교육 유튜버, 28세)
- **니즈**: 주 3회 유튜브 콘텐츠 소재 필요
- **행동**: 발행된 기사 읽기 → 대본 참고 → 영상 제작
- **기대**: 강렬한 비주얼과 명확한 정보 구조

#### Persona 3: 관리자 (jpmjkim23@gmail.com, 1명)
- **니즈**: 뉴스 크롤링 → 파인만 변환 → 발행을 효율적으로
- **행동**: 구글 로그인 → 대시보드 → 뉴스 선택 → 생성 → 발행
- **권한**: API 설정, 사용자 관리, 모든 관리 기능 접근

---

## 2. 디자인 철학 - Neo-Brutalism

### 2.1 Neo-Brutalism이란?

**네오 부루탈리즘(Neo-Brutalism)**은 다음 요소들로 구성된 현대적 디자인 스타일입니다:

#### 핵심 특징:
- **굵은 검은 테두리** (3-5px solid black borders)
- **강렬한 그림자** (8-12px offset shadow, no blur)
- **대담한 색상** (형광색, 원색 사용)
- **타이포그래피** (큰 폰트, 굵은 글씨, 명확한 대비)
- **기하학적 형태** (사각형, 직선 중심)
- **과감한 여백** (충분한 padding과 spacing)

### 2.2 디자인 시스템

#### 색상 팔레트:
```css
/* Primary Colors - 강렬한 원색 */
--primary: #FF3366;        /* 핫 핑크 */
--secondary: #00D9FF;      /* 시안 블루 */
--accent: #FFE500;         /* 일렉트릭 옐로우 */
--success: #00FF85;        /* 네온 그린 */
--warning: #FF9500;        /* 오렌지 */
--error: #FF0055;          /* 레드 */

/* Neutrals */
--black: #000000;
--white: #FFFFFF;
--gray-100: #F5F5F5;
--gray-900: #1A1A1A;

/* Backgrounds */
--bg-primary: #FFFFFF;
--bg-secondary: #F5F5F5;
--bg-accent: #FFE500;
```

#### 타이포그래피:
```css
/* Headings - Space Grotesk 폰트 */
--font-heading: 'Space Grotesk', sans-serif;
--h1: 64px / 900 weight;
--h2: 48px / 800 weight;
--h3: 32px / 700 weight;

/* Body - Inter 폰트 */
--font-body: 'Inter', sans-serif;
--body-lg: 20px / 500 weight;
--body-md: 16px / 400 weight;
--body-sm: 14px / 400 weight;
```

#### 컴포넌트 스타일:
```css
/* 버튼 */
.neo-button {
  border: 4px solid black;
  box-shadow: 8px 8px 0px black;
  padding: 16px 32px;
  font-weight: 700;
  transition: transform 0.1s, box-shadow 0.1s;
}

.neo-button:hover {
  transform: translate(2px, 2px);
  box-shadow: 6px 6px 0px black;
}

.neo-button:active {
  transform: translate(4px, 4px);
  box-shadow: 4px 4px 0px black;
}

/* 카드 */
.neo-card {
  border: 4px solid black;
  box-shadow: 12px 12px 0px black;
  padding: 32px;
  background: white;
}

/* 입력 필드 */
.neo-input {
  border: 3px solid black;
  padding: 12px 16px;
  font-size: 16px;
  font-weight: 500;
}

.neo-input:focus {
  outline: 4px solid var(--accent);
  outline-offset: 2px;
}
```

### 2.3 Neo-Brutalism Skill 활용

**Neo-Brutalism Designer Skill 사용**:
- 모든 UI 컴포넌트는 `neo-brutalism-designer` skill을 통해 생성
- artifacts-builder와 shadcn/ui 통합으로 인터랙티브 웹페이지 제작
- 굵은 테두리, 강렬한 그림자, 대담한 색상 자동 적용

---

## 3. 시스템 아키텍처

### 3.1 전체 아키텍처 다이어그램 (v2.0)

```
┌─────────────────────────────────────────────────────────────┐
│                    사용자 계층 (3가지 역할)                   │
├─────────────────────────────────────────────────────────────┤
│  1. 일반 사용자 (Google 로그인)                               │
│     - 뉴스 읽기 (발행된 기사만)                               │
│     - 뉴스레터 구독 (이메일 입력)                             │
│                                                               │
│  2. 관리자 (jpmjkim23@gmail.com ONLY)                       │
│     - 뉴스 크롤링 실행                                        │
│     - 파인만 스타일 생성                                      │
│     - 기사 편집 및 발행                                       │
│     - API 키 설정 (xAI Grok, Resend 등)                     │
│     - 사용자 권한 관리                                        │
│                                                               │
│  3. 뉴스레터 구독자 (비로그인)                                │
│     - 이메일만 입력하여 구독                                  │
│     - 주간 뉴스레터 수신                                      │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│              프론트엔드 (Vercel) - Neo-Brutalism              │
├─────────────────────────────────────────────────────────────┤
│  Next.js 14 (App Router) + neo-brutalism-designer skill     │
│                                                               │
│  📱 Public Pages (모두 접근):                                 │
│  ├── /[locale]                  # 홈 (최신 뉴스 6개)         │
│  ├── /[locale]/posts            # 뉴스 목록 (무한 스크롤)    │
│  ├── /[locale]/posts/[slug]     # 뉴스 상세                  │
│  ├── /[locale]/newsletter       # 뉴스레터 구독 페이지       │
│  └── /[locale]/login            # Google 로그인              │
│                                                               │
│  🔒 Admin Pages (관리자만):                                  │
│  ├── /[locale]/admin            # 대시보드                   │
│  ├── /[locale]/admin/crawler    # 크롤링 관리                │
│  ├── /[locale]/admin/editor     # 편집기                     │
│  ├── /[locale]/admin/settings   # API 키 설정                │
│  └── /[locale]/admin/users      # 사용자 관리                │
│                                                               │
│  Components (Neo-Brutalism):                                  │
│  ├── NewsCard.tsx               # 뉴스 카드 (굵은 테두리)    │
│  ├── FeynmanQuestionBox.tsx     # 질문 박스 (강렬한 색상)    │
│  ├── NeoButton.tsx              # 네오 부루탈 버튼           │
│  └── NewsletterForm.tsx         # 구독 폼 (대담한 디자인)    │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                  API 계층 (Next.js API Routes)               │
├─────────────────────────────────────────────────────────────┤
│  /api/auth/[...nextauth]        # NextAuth.js (Google)       │
│  /api/crawl                     # 뉴스 크롤링 (관리자만)     │
│  /api/generate                  # 파인만 생성 (관리자만)     │
│  /api/publish                   # 발행 (관리자만)            │
│  /api/newsletter/subscribe      # 구독 (모두)                │
│  /api/admin/settings            # API 키 저장 (관리자만)     │
│  /api/admin/users               # 사용자 관리 (관리자만)     │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌──────────────────┬──────────────────┬────────────────────────┐
│   Firebase       │   xAI Grok       │   External APIs        │
│   (Google)       │   (동적 설정)     │                        │
├──────────────────┼──────────────────┼────────────────────────┤
│ Firestore        │ API 키:          │ Resend (Email)         │
│ - raw_news       │ 관리자 페이지에서 │ - 뉴스레터 발송         │
│ - feynman_...    │ 암호화하여 저장   │                        │
│ - newsletters    │ (AES-256-GCM)    │ Twitter API (선택)     │
│ - subscribers    │                  │ - 트윗 발행             │
│ - users          │ Fallback:        │                        │
│ - api_keys       │ - GPT-4 Turbo    │ RSS Feeds (14개 소스)  │
│   (암호화됨)      │                  │                        │
│                  │                  │                        │
│ Auth             │                  │                        │
│ - Google OAuth   │                  │                        │
│ - 관리자 화이트리스트│                │                        │
│                  │                  │                        │
│ Functions        │                  │                        │
│ - scheduledCrawl │                  │                        │
│ - newsletter     │                  │                        │
└──────────────────┴──────────────────┴────────────────────────┘
```

### 3.2 기술 스택 (v2.0 Updated)

#### Frontend

| 기술 | 버전 | 용도 | 선택 이유 |
|------|------|------|----------|
| **Next.js** | 14.1.0 | React 프레임워크 | App Router, SSR/SSG |
| **TypeScript** | 5.3.3 | 타입 안전성 | 대규모 프로젝트 유지보수 |
| **TailwindCSS** | 3.4.1 | 스타일링 | Neo-Brutalism 구현 용이 |
| **shadcn/ui** | latest | UI 컴포넌트 | Neo-Brutalism 커스터마이징 |
| **Neo-Brutalism Skill** | - | 디자인 시스템 | Claude Code Skill 활용 |
| **NextAuth.js** | 4.24.5 | Google OAuth | 간편한 구글 로그인 |
| **TipTap** | 2.1.16 | WYSIWYG 에디터 | Markdown 호환 |
| **next-intl** | 3.7.0 | 다국어 (ko/en) | App Router 최적화 |

#### Backend & Security

| 기술 | 용도 | 보안 특징 |
|------|------|----------|
| **Firebase Firestore** | 데이터베이스 | Rules 기반 접근 제어 |
| **Firebase Auth** | Google 로그인 | OAuth 2.0 인증 |
| **crypto-js** | AES-256-GCM 암호화 | API 키 암호화 저장 |
| **Firebase Functions** | 서버리스 백엔드 | 자동 스케일링 |

#### AI & External Services

| 기술 | 용도 | 설정 방식 |
|------|------|----------|
| **xAI Grok-4-1-fast** | 파인만 생성 | 관리자 페이지에서 동적 설정 |
| **Resend** | 이메일 발송 | 관리자 페이지에서 API 키 입력 |

---

## 4. 인증 및 권한 시스템

### 4.1 Google 로그인 (NextAuth.js)

#### 사용자 가입 플로우:
```
[홈 페이지] → [로그인 버튼] → [Google 계정 선택]
    ↓
[Google OAuth 승인] → [Firebase에 사용자 정보 저장]
    ↓
[users 컬렉션에 uid, email, name, photoURL 저장]
    ↓
[역할 확인: email === 'jpmjkim23@gmail.com' ? 'admin' : 'user']
    ↓
[홈으로 리다이렉트]
```

#### NextAuth 설정:
```typescript
// app/api/auth/[...nextauth]/route.ts
import NextAuth from 'next-auth';
import GoogleProvider from 'next-auth/providers/google';
import { adminDb } from '@/lib/firebase-admin';

export const authOptions = {
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
    }),
  ],
  callbacks: {
    async signIn({ user, account }) {
      // Firestore에 사용자 정보 저장
      await adminDb.collection('users').doc(user.id).set({
        uid: user.id,
        email: user.email,
        name: user.name,
        photoURL: user.image,
        role: user.email === 'jpmjkim23@gmail.com' ? 'admin' : 'user',
        createdAt: new Date(),
      }, { merge: true });

      return true;
    },
    async session({ session, token }) {
      const userDoc = await adminDb.collection('users').doc(token.sub!).get();
      session.user.role = userDoc.data()?.role || 'user';
      return session;
    },
  },
};

const handler = NextAuth(authOptions);
export { handler as GET, handler as POST };
```

### 4.2 권한 레벨

| 역할 | 조건 | 권한 |
|------|------|------|
| **admin** | `email === 'jpmjkim23@gmail.com'` | 모든 기능 접근 |
| **user** | 기타 Google 로그인 사용자 | 뉴스 읽기, 뉴스레터 구독 |
| **guest** | 비로그인 사용자 | 뉴스 읽기, 뉴스레터 구독 |

### 4.3 관리자 전용 기능

#### `/admin` 경로 보호:
```typescript
// middleware.ts
import { getToken } from 'next-auth/jwt';
import { NextRequest, NextResponse } from 'next/server';

export async function middleware(request: NextRequest) {
  const token = await getToken({ req: request });

  // /admin 경로는 관리자만 접근
  if (request.nextUrl.pathname.startsWith('/admin')) {
    if (!token || token.email !== 'jpmjkim23@gmail.com') {
      return NextResponse.redirect(new URL('/login', request.url));
    }
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/admin/:path*', '/api/admin/:path*', '/api/crawl', '/api/generate', '/api/publish'],
};
```

### 4.4 관리자 페이지 - 사용자 관리

관리자는 `/admin/users`에서 다음 작업 가능:
- 사용자 목록 조회 (이메일, 이름, 가입일, 역할)
- 역할 변경 (`user` ↔ `admin`)
- 사용자 비활성화/활성화
- 사용자 삭제 (soft delete)

---

## 5. 데이터베이스 설계

### 5.1 Firestore Collections (v2.0 Updated)

#### Collection: `users` (사용자) **[NEW]**

```typescript
{
  id: string;                    // 문서 ID (Firebase Auth UID)
  uid: string;                   // Firebase Auth UID
  email: string;                 // 이메일 (unique)
  name: string;                  // 이름
  photoURL: string;              // 프로필 사진 (Google)

  role: "admin" | "user";        // 역할
  status: "active" | "inactive"; // 상태

  createdAt: Timestamp;          // 가입일
  lastLoginAt: Timestamp;        // 마지막 로그인

  // 관리자 추가 정보
  managedBy?: string;            // 누가 권한 부여했는지
  permissionGrantedAt?: Timestamp;
}
```

**인덱스:**
- `email` (unique)
- `role + status`

---

#### Collection: `api_keys` (API 키 암호화 저장) **[NEW]**

```typescript
{
  id: string;                    // 문서 ID
  service: "xai" | "resend" | "twitter";

  // 암호화된 API 키
  encryptedKey: string;          // AES-256-GCM 암호화
  iv: string;                    // 초기화 벡터

  // 메타데이터
  createdBy: string;             // 관리자 UID
  createdAt: Timestamp;
  updatedAt: Timestamp;

  // 상태
  status: "active" | "inactive";
  lastUsedAt: Timestamp;
}
```

**보안:**
- API 키는 AES-256-GCM으로 암호화하여 저장
- 암호화 키는 Firebase Secret Manager에 저장 (환경 변수)
- 복호화는 서버사이드에서만 수행

---

#### Collection: `raw_news` (원본 뉴스) - 기존 유지

```typescript
{
  id: string;
  title: string;
  content: string;
  source: string;
  url: string;
  publishedAt: Timestamp;
  crawledAt: Timestamp;

  category: "AI" | "Education" | "AI+Education";
  country: "KR" | "US";
  status: "pending" | "selected" | "processed" | "published";
  keywords: string[];
}
```

---

#### Collection: `feynman_articles` (파인만 기사) - 기존 유지

(이전과 동일)

---

#### Collection: `newsletters` (뉴스레터) - 기존 유지

(이전과 동일)

---

#### Collection: `subscribers` (구독자) - **이메일만 수집**

```typescript
{
  id: string;
  email: string;                 // unique (이메일만!)

  subscribedAt: Timestamp;
  unsubscribedAt?: Timestamp;

  status: "active" | "unsubscribed" | "bounced";

  preferences: {
    frequency: "weekly" | "biweekly";
    language: "ko" | "en";
  };

  // 통계
  totalOpens: number;
  totalClicks: number;
  lastOpenedAt?: Timestamp;
}
```

**주요 변경:**
- `name` 필드 제거 → 이메일만 수집하여 간편화

---

## 6. 기능 명세

### 6.1 핵심 기능 (v2.0 Updated)

| 기능 ID | 기능명 | 설명 | 역할 | 상태 |
|---------|--------|------|------|------|
| **인증 & 권한** |||
| A001 | Google 로그인 | NextAuth.js 기반 OAuth | 모두 | 📋 Phase 1 |
| A002 | 역할 기반 접근 제어 | admin/user 구분 | 시스템 | 📋 Phase 1 |
| A003 | 사용자 관리 | 관리자가 권한 부여/변경 | 관리자 | 📋 Phase 4 |
| **공개 기능** ||||
| P001 | 뉴스 목록 조회 | 발행된 뉴스만 표시 | 모두 | 📋 Phase 3 |
| P002 | 뉴스 상세 보기 | 파인만 스타일 기사 읽기 | 모두 | 📋 Phase 3 |
| P003 | 뉴스레터 구독 | 이메일 입력만으로 구독 | 모두 | 📋 Phase 3 |
| **관리자 기능** ||||
| M001 | 뉴스 크롤링 | 수동 크롤링 트리거 | 관리자 | ✅ 완료 |
| M002 | 파인만 변환 | Grok API 호출 | 관리자 | ✅ 완료 |
| M003 | 기사 편집 | WYSIWYG 편집기 | 관리자 | 📋 Phase 4 |
| M004 | 기사 발행 | 블로그 발행 | 관리자 | 📋 Phase 4 |
| M005 | API 키 설정 | xAI, Resend API 관리 | 관리자 | 📋 Phase 4 |
| **자동화** ||||
| S001 | 자동 크롤링 | 매일 09:00 KST | 시스템 | 📋 Phase 5 |
| S002 | 뉴스레터 발송 | 매주 일요일 20:00 KST | 시스템 | 📋 Phase 5 |

---

## 7. 단계별 개발 계획 (6 Phases)

### 📊 개발 철학: 소프트 랜딩 (Soft Landing)

각 Phase는 독립적으로 테스트 가능하며, 이전 Phase가 완료된 후 다음 Phase로 진행합니다.

---

### 🏗️ Phase 1: 보안 & 인증 시스템 (1-2일)

**목표**: 안전한 로그인 및 권한 관리 시스템 구축

#### 작업 목록:
1. **NextAuth.js 설정**
   - [ ] Google OAuth Provider 추가
   - [ ] `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` 환경 변수 설정
   - [ ] `/api/auth/[...nextauth]/route.ts` 구현

2. **Firestore `users` 컬렉션 생성**
   - [ ] 사용자 정보 저장 로직 (signIn callback)
   - [ ] 역할 자동 할당 (`jpmjkim23@gmail.com` → admin)

3. **Middleware 구현**
   - [ ] `/admin` 경로 보호
   - [ ] 관리자 권한 체크 (email 검증)

4. **로그인 페이지 (Neo-Brutalism)**
   - [ ] `app/[locale]/login/page.tsx` 생성
   - [ ] "Sign in with Google" 버튼 (굵은 테두리, 강렬한 색상)

5. **테스트**
   - [ ] 구글 로그인 성공 확인
   - [ ] 관리자/일반 사용자 역할 분리 확인
   - [ ] `/admin` 접근 제어 확인

**산출물**:
- ✅ Google 로그인 작동
- ✅ 관리자 페이지 접근 제한
- ✅ Firestore `users` 컬렉션 생성

---

### 🎨 Phase 2: Neo-Brutalism 디자인 시스템 (2-3일)

**목표**: 전체 사이트에 적용할 디자인 시스템 구축

#### 작업 목록:
1. **글로벌 CSS 설정**
   - [ ] `app/globals.css`에 Neo-Brutalism 변수 정의
   - [ ] Space Grotesk, Inter 폰트 로드
   - [ ] 색상, 그림자, 테두리 시스템 구축

2. **재사용 컴포넌트 (neo-brutalism-designer skill 활용)**
   - [ ] `NeoButton.tsx` - 대담한 버튼
   - [ ] `NeoCard.tsx` - 뉴스 카드
   - [ ] `NeoInput.tsx` - 입력 필드
   - [ ] `NeoBadge.tsx` - 카테고리 배지
   - [ ] `NeoLayout.tsx` - 공통 레이아웃

3. **헤더 & 네비게이션**
   - [ ] Neo-Brutalism 스타일 헤더
   - [ ] 언어 선택기 (KO/EN)
   - [ ] 로그인/로그아웃 버튼

4. **테스트**
   - [ ] 모든 컴포넌트 시각적 확인
   - [ ] 반응형 디자인 체크 (모바일/태블릿/데스크톱)
   - [ ] 접근성 검증

**산출물**:
- ✅ Neo-Brutalism 디자인 시스템 완성
- ✅ 재사용 가능한 UI 컴포넌트 라이브러리

---

### 👥 Phase 3: 사용자 페이지 개발 (3-4일)

**목표**: 일반 사용자가 뉴스를 읽고 구독할 수 있는 공개 페이지 구축

#### 작업 목록:
1. **홈 페이지 (`/[locale]/page.tsx`)**
   - [ ] 최신 뉴스 6개 카드 (Neo-Brutalism 스타일)
   - [ ] 뉴스레터 구독 폼 (이메일 입력)
   - [ ] Hero 섹션 (대담한 타이포그래피)

2. **뉴스 목록 페이지 (`/[locale]/posts/page.tsx`)**
   - [ ] 무한 스크롤 또는 페이지네이션
   - [ ] 카테고리 필터 (AI, Education, AI+Education)
   - [ ] 국가 필터 (KR, US)

3. **뉴스 상세 페이지 (`/[locale]/posts/[slug]/page.tsx`)**
   - [ ] 파인만 제목, 요약, 본문 (Markdown 렌더링)
   - [ ] 파인만 질문 3개 (강렬한 색상 박스)
   - [ ] 공유 버튼 (Twitter, Facebook)

4. **뉴스레터 구독 API (`/api/newsletter/subscribe`)**
   - [ ] 이메일 검증 (유효성 체크)
   - [ ] Firestore `subscribers` 컬렉션에 저장
   - [ ] 중복 체크 및 에러 처리

5. **테스트**
   - [ ] 뉴스 읽기 기능 확인
   - [ ] 뉴스레터 구독 성공 확인
   - [ ] SEO 메타태그 확인

**산출물**:
- ✅ 일반 사용자용 완전한 공개 페이지
- ✅ 뉴스레터 구독 기능

---

### 🔧 Phase 4: 관리자 시스템 개발 (4-5일)

**목표**: 관리자가 뉴스를 생산하고 발행할 수 있는 시스템 구축

#### 작업 목록:
1. **관리자 대시보드 (`/[locale]/admin/page.tsx`)**
   - [ ] 통계 카드 (오늘의 뉴스, 발행된 기사, 구독자 수)
   - [ ] 최근 활동 로그
   - [ ] 빠른 액션 버튼

2. **크롤러 관리 페이지 (`/[locale]/admin/crawler/page.tsx`)**
   - [ ] 오늘 크롤링된 뉴스 목록
   - [ ] 체크박스 다중 선택 (최대 10개)
   - [ ] 필터: 카테고리, 국가, 소스
   - [ ] "파인만 스타일 변환" 버튼
   - [ ] 로딩 상태 표시 (30-60초)

3. **편집기 (`/[locale]/admin/editor/page.tsx`)**
   - [ ] TipTap WYSIWYG 에디터
   - [ ] 실시간 미리보기 (오른쪽 패널)
   - [ ] 파인만 제목, 요약, 본문, 질문 편집
   - [ ] 저장/발행 버튼

4. **API 키 설정 페이지 (`/[locale]/admin/settings/page.tsx`)**
   - [ ] xAI Grok API 키 입력
   - [ ] Resend API 키 입력
   - [ ] AES-256-GCM 암호화 후 Firestore 저장
   - [ ] 마스킹 표시 (********)
   - [ ] 테스트 버튼 (API 연결 확인)

5. **사용자 관리 페이지 (`/[locale]/admin/users/page.tsx`)**
   - [ ] 사용자 목록 (이메일, 이름, 역할, 가입일)
   - [ ] 역할 변경 (user ↔ admin)
   - [ ] 사용자 비활성화/활성화

6. **API 라우트 구현**
   - [ ] `/api/admin/settings` - API 키 저장/조회
   - [ ] `/api/admin/users` - 사용자 관리
   - [ ] `/api/publish` - 기사 발행

7. **테스트**
   - [ ] 전체 워크플로우 (크롤링 → 생성 → 편집 → 발행)
   - [ ] API 키 암호화/복호화 확인
   - [ ] 권한 검증

**산출물**:
- ✅ 완전한 관리자 시스템
- ✅ API 키 암호화 저장 기능
- ✅ 사용자 관리 기능

---

### 🤖 Phase 5: AI 통합 & 자동화 (2-3일)

**목표**: Grok API 통합 및 자동 크롤링/뉴스레터 발송

#### 작업 목록:
1. **Grok API 클라이언트 개선**
   - [ ] Firestore에서 암호화된 API 키 조회
   - [ ] 복호화 로직 구현
   - [ ] Fallback (GPT-4 Turbo) 로직

2. **Firebase Functions 구현**
   - [ ] `functions/src/crawl.ts` - 스케줄 크롤링
   - [ ] `functions/src/newsletter.ts` - 뉴스레터 발송

3. **Vercel Cron 설정**
   - [ ] `vercel.json`에 크롤링 스케줄 추가
   - [ ] `/api/crawl` 엔드포인트 연결

4. **뉴스레터 HTML 템플릿**
   - [ ] React Email 템플릿 작성
   - [ ] Resend API 연동

5. **테스트**
   - [ ] 수동 크롤링 성공 확인
   - [ ] 파인만 생성 성공 확인
   - [ ] 뉴스레터 발송 테스트

**산출물**:
- ✅ xAI Grok API 완전 통합
- ✅ 자동 크롤링 시스템
- ✅ 자동 뉴스레터 발송

---

### 🚀 Phase 6: 테스트, 최적화 & 배포 (2-3일)

**목표**: 프로덕션 배포 및 성능 최적화

#### 작업 목록:
1. **통합 테스트**
   - [ ] E2E 테스트 (Playwright)
   - [ ] 사용자 시나리오 테스트
   - [ ] 관리자 워크플로우 테스트

2. **성능 최적화**
   - [ ] 이미지 최적화 (Next.js Image)
   - [ ] 코드 스플리팅
   - [ ] 번들 크기 분석

3. **SEO 최적화**
   - [ ] 메타태그 추가
   - [ ] sitemap.xml 생성
   - [ ] robots.txt 설정

4. **Vercel 배포**
   - [ ] 환경 변수 설정 (프로덕션)
   - [ ] 도메인 연결
   - [ ] HTTPS 설정

5. **Firebase 배포**
   - [ ] Firestore Rules 배포
   - [ ] Firebase Functions 배포

6. **모니터링 설정**
   - [ ] Vercel Analytics
   - [ ] Firebase Console 모니터링

**산출물**:
- ✅ 프로덕션 배포 완료
- ✅ 성능 최적화 완료
- ✅ 모니터링 시스템 가동

---

## 8. API 명세

### 8.1 인증 API

#### POST `/api/auth/[...nextauth]`

**NextAuth.js 엔드포인트** (Google OAuth)

---

### 8.2 공개 API

#### POST `/api/newsletter/subscribe`

**요청**:
```typescript
{
  email: string;
  language: "ko" | "en";
  frequency: "weekly" | "biweekly";
}
```

**응답**:
```typescript
{
  success: boolean;
  message: string;
}
```

---

### 8.3 관리자 API (권한 필요)

#### POST `/api/crawl`

**권한**: admin only

**요청**:
```typescript
{
  sources?: ("korea" | "usa" | "all")[];
}
```

**응답**:
```typescript
{
  success: boolean;
  totalNews: number;
  newNews: number;
  errors: string[];
}
```

---

#### POST `/api/generate`

**권한**: admin only

**요청**:
```typescript
{
  newsIds: string[];   // 최대 10개
  language: "ko" | "en";
}
```

**응답**:
```typescript
{
  success: boolean;
  articles: Array<{
    newsId: string;
    articleId: string;
    feynmanTitle: string;
  }>;
  errors: Array<{ newsId: string; error: string }>;
}
```

---

#### POST `/api/publish`

**권한**: admin only

**요청**:
```typescript
{
  articleId: string;
  platforms: ("blog" | "twitter")[];
  addToNewsletter: boolean;
}
```

**응답**:
```typescript
{
  success: boolean;
  results: {
    blog?: { published: boolean; url: string };
    twitter?: { published: boolean; url?: string };
  };
}
```

---

#### POST `/api/admin/settings`

**권한**: admin only

**요청**:
```typescript
{
  service: "xai" | "resend" | "twitter";
  apiKey: string;  // 평문 (서버에서 암호화)
}
```

**응답**:
```typescript
{
  success: boolean;
  message: string;
}
```

**보안**: API 키는 AES-256-GCM으로 암호화 후 Firestore 저장

---

#### GET `/api/admin/settings`

**권한**: admin only

**응답**:
```typescript
{
  success: boolean;
  settings: {
    xai: { exists: boolean; lastUpdated: string };
    resend: { exists: boolean; lastUpdated: string };
  };
}
```

**주의**: 실제 API 키는 반환하지 않음 (마스킹 처리)

---

#### GET/POST `/api/admin/users`

**권한**: admin only

**GET 응답**:
```typescript
{
  users: Array<{
    uid: string;
    email: string;
    name: string;
    role: "admin" | "user";
    status: "active" | "inactive";
    createdAt: string;
  }>;
}
```

**POST 요청** (역할 변경):
```typescript
{
  uid: string;
  role: "admin" | "user";
}
```

---

## 9. UI/UX 설계 (Neo-Brutalism)

### 9.1 홈 페이지 (Neo-Brutalism 스타일)

```
┌────────────────────────────────────────────────────────────┐
│  ╔═══════════════════════════════════════════════════════╗  │
│  ║  🎓 AI EDU NEWS  [KO/EN] [LOGIN]                      ║  │
│  ╚═══════════════════════════════════════════════════════╝  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  🚀 UNDERSTAND AI/EDU NEWS                          │  │
│  │  IN FEYNMAN WAY                                     │  │
│  │  (Space Grotesk 64px, 굵은 글씨)                    │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  [이메일 입력]  [🔔 구독하기]                       │  │
│  │  (굵은 검은 테두리, 강렬한 그림자)                   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
├────────────────────────────────────────────────────────────┤
│  📰 LATEST NEWS                                            │
│                                                            │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐            │
│  │ ╔════════╗ │ │ ╔════════╗ │ │ ╔════════╗ │            │
│  │ ║ 🇰🇷 AI  ║ │ │ ║ 🇺🇸 EDU ║ │ │ ║ 🇰🇷 AI+ ║ │            │
│  │ ╚════════╝ │ │ ╚════════╝ │ │ ╚════════╝ │            │
│  │  제목...   │ │  제목...   │ │  제목...   │            │
│  │  (4px 테두리│ │  (12px 그림자│ │  (형광색)  │            │
│  └────────────┘ └────────────┘ └────────────┘            │
│                                                            │
│  [MORE NEWS →]                                             │
│  (Neo-Button: 굵은 테두리, 강렬한 색상)                    │
└────────────────────────────────────────────────────────────┘
```

### 9.2 뉴스 상세 페이지 (Neo-Brutalism)

```
┌────────────────────────────────────────────────────────────┐
│  ╔═══════════════════════════════════════════════════════╗  │
│  ║  🎓 AI EDU NEWS                                       ║  │
│  ╚═══════════════════════════════════════════════════════╝  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  🤔 왜 AI는 우리 숙제를 도와줄 수 있을까?                   │
│  (Space Grotesk 48px, 굵은 글씨)                          │
│  ───────────────────────────────────────────────────────  │
│  📅 2025-11-23 · 🇺🇸 TechCrunch · ⭐⭐⭐ 중급            │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  💡 한 줄 요약                                       │  │
│  │  AI는 마치 무엇이든 외우는 마법 도서관과 같아요...   │  │
│  │  (노란색 배경, 검은 테두리 4px)                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│  [본문 Markdown]                                           │
│  ## 1단계: 선택                                            │
│  ...                                                       │
│                                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│  🎓 파인만 교수의 질문                                     │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  1. 왜 이게 작동하는 거지?                           │  │
│  │  💡 파인만은 항상 근본 원리를 이해하려 했습니다...    │  │
│  │  (핑크색 배경, 검은 테두리 4px, 그림자 12px)         │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  2. 이걸 어디에 쓸 수 있을까?                        │  │
│  │  💡 실용성을 중시한 파인만은...                      │  │
│  │  (시안 배경, 검은 테두리 4px, 그림자 12px)           │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  3. 만약 반대라면 어떻게 될까?                       │  │
│  │  💡 역으로 생각하는 것을...                          │  │
│  │  (연두색 배경, 검은 테두리 4px, 그림자 12px)         │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                            │
│  #AI #교육 #ChatGPT                                        │
│  [SHARE] [TWITTER] [COPY LINK]                             │
│  (Neo-Buttons: 굵은 테두리, 강렬한 색상)                   │
└────────────────────────────────────────────────────────────┘
```

### 9.3 관리자 대시보드 (Neo-Brutalism)

```
┌────────────────────────────────────────────────────────────┐
│  ╔═══════════════════════════════════════════════════════╗  │
│  ║  🎓 ADMIN DASHBOARD                                   ║  │
│  ║  👤 jpmjkim23@gmail.com [LOGOUT]                     ║  │
│  ╚═══════════════════════════════════════════════════════╝  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ ╔══════╗ │ │ ╔══════╗ │ │ ╔══════╗ │ │ ╔══════╗ │    │
│  │ ║  42  ║ │ │ ║  90  ║ │ │ ║  12  ║ │ │ ║ 500  ║ │    │
│  │ ╚══════╝ │ │ ╚══════╝ │ │ ╚══════╝ │ │ ╚══════╝ │    │
│  │   뉴스   │ │   기사   │ │   대기   │ │   구독   │    │
│  │ (핑크배경)│ │ (시안배경)│ │ (노랑배경)│ │ (초록배경)│    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  📰 오늘의 뉴스 (42개) [🔄 새로고침] [🚀 크롤링]           │
│                                                            │
│  [전체선택] 필터: [AI] [교육] [🇰🇷 KR] [🇺🇸 US]            │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ ☑ ChatGPT 교육 활용 가이드 발표                      │  │
│  │    🇺🇸 TechCrunch · 3시간 전 · AI                   │  │
│  │    (Neo-Card: 4px 테두리, 12px 그림자)               │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ ☑ 서울시교육청, AI 교사 연수 확대                    │  │
│  │    🇰🇷 에듀프레스 · 5시간 전 · 교육                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ □ Gemini 1.5 Pro 출시                                │  │
│  │    🇺🇸 The Verge · 6시간 전 · AI                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ──────────────────────────────────────────────────────── │
│  선택: 2개                                                 │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  🎓 파인만 스타일로 변환 (2개)                       │  │
│  │  (Neo-Button: 핑크 배경, 검은 테두리 5px, 그림자)   │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

## 10. 보안 및 암호화

### 10.1 API 키 암호화 시스템

#### AES-256-GCM 암호화:

```typescript
// lib/encryption.ts
import CryptoJS from 'crypto-js';

const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY!; // 환경 변수

export function encryptApiKey(apiKey: string): {
  encrypted: string;
  iv: string;
} {
  const iv = CryptoJS.lib.WordArray.random(16);

  const encrypted = CryptoJS.AES.encrypt(apiKey, ENCRYPTION_KEY, {
    iv: iv,
    mode: CryptoJS.mode.GCM,
    padding: CryptoJS.pad.Pkcs7,
  });

  return {
    encrypted: encrypted.toString(),
    iv: iv.toString(CryptoJS.enc.Hex),
  };
}

export function decryptApiKey(encryptedKey: string, ivHex: string): string {
  const iv = CryptoJS.enc.Hex.parse(ivHex);

  const decrypted = CryptoJS.AES.decrypt(encryptedKey, ENCRYPTION_KEY, {
    iv: iv,
    mode: CryptoJS.mode.GCM,
    padding: CryptoJS.pad.Pkcs7,
  });

  return decrypted.toString(CryptoJS.enc.Utf8);
}
```

### 10.2 Firestore Security Rules (v2.0)

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 공개 읽기: 발행된 기사만
    match /feynman_articles/{article} {
      allow read: if resource.data.status == 'published';
      allow write: if request.auth.token.email == 'jpmjkim23@gmail.com';
    }

    // 뉴스레터 구독: 누구나 생성 가능
    match /subscribers/{subscriber} {
      allow create: if true;
      allow read, update, delete: if request.auth.uid == subscriber
                                   || request.auth.token.email == 'jpmjkim23@gmail.com';
    }

    // 사용자: 본인 또는 관리자만 읽기
    match /users/{user} {
      allow read: if request.auth.uid == user
                  || request.auth.token.email == 'jpmjkim23@gmail.com';
      allow write: if request.auth.token.email == 'jpmjkim23@gmail.com';
    }

    // API 키: 관리자만 접근
    match /api_keys/{key} {
      allow read, write: if request.auth.token.email == 'jpmjkim23@gmail.com';
    }

    // 나머지: 관리자만 전체 접근
    match /{document=**} {
      allow read, write: if request.auth.token.email == 'jpmjkim23@gmail.com';
    }
  }
}
```

---

## 11. 배포 및 운영

### 11.1 환경 변수 (Vercel)

```bash
# Firebase Client SDK
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=masteroflearning-a06c6.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=masteroflearning-a06c6
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=...
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...

# Firebase Admin SDK
FIREBASE_ADMIN_PROJECT_ID=masteroflearning-a06c6
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-...
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Google OAuth (NextAuth.js)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=your_nextauth_secret

# 암호화 키 (API 키 암호화용)
ENCRYPTION_KEY=your_32_character_secret_key_here

# 기타
ADMIN_EMAIL=jpmjkim23@gmail.com
```

### 11.2 Vercel 배포

```bash
# Vercel CLI 설치
npm install -g vercel

# 프로젝트 연결
vercel

# 환경 변수 설정
vercel env add GOOGLE_CLIENT_ID production
vercel env add ENCRYPTION_KEY production

# 프로덕션 배포
vercel --prod
```

### 11.3 Firebase 배포

```bash
# Firebase CLI 설치
npm install -g firebase-tools

# 로그인
firebase login

# 프로젝트 연결
firebase use masteroflearning-a06c6

# Firestore Rules 배포
firebase deploy --only firestore:rules

# Functions 배포
firebase deploy --only functions
```

---

## 12. 비용 예측

### 12.1 월간 비용 (구독자 1,000명 기준)

| 서비스 | 사용량 | 무료 한도 | 초과 비용 | 월 비용 |
|--------|--------|-----------|----------|---------|
| **Vercel** | 100GB 대역폭 | 100GB | - | $0 |
| **Firebase Firestore** | 30K reads/day | 50K/day | - | $0 |
| **Firebase Auth** | Google OAuth | 무제한 | - | $0 |
| **Firebase Functions** | 60 invocations/month | 2M/month | - | $0 |
| **xAI Grok** | 90 기사/월 (~500K tokens) | - | $0.50/1M in + $1.50/1M out | $15 |
| **Resend** | 4,000 emails/month | 3,000 | 1,000 초과 | $5 |
| **총계** | | | | **$20/월** |

---

## 📝 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 1.0.0 | 2025-11-23 | 초안 작성 | Claude (Anthropic) |
| 2.0.0 | 2025-11-23 | 전면 개편 (Neo-Brutalism, Google 로그인, 단계적 개발 계획) | Claude (Anthropic) |

---

## 📞 문의

- **GitHub**: [reallygood83/masteroflearning](https://github.com/reallygood83/masteroflearning)
- **Email**: jpmjkim23@gmail.com
- **관리자**: jpmjkim23@gmail.com (Google 로그인)

---

**끝**
